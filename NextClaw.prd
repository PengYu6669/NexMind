# NextClaw 业务闭环 PRD（V1）

## 1. 文档信息

- 产品名称：ima-claw / NextClaw
- 文档版本：V1.0
- 目标：将 AI 能力转化为可验证的学习业务能力
- 周期建议：4-6 周

## 2. 背景与问题

当前系统已经具备 AI 对话、知识库、知识图谱、Agent 执行流和 MCP 工具接入能力，但业务闭环仍不完整，主要表现为：

1. 智能流程可运行，但业务目标（学会、记住、完成计划）约束不足。
2. 单 Agent 过载，缺少角色分工与协作边界。
3. 执行策略仍偏 Prompt 驱动，规则化不足。
4. 结果缺少统一评估，难以稳定迭代。

## 3. 产品目标

构建学习业务闭环：输入知识 -> 产出学习动作 -> 执行复习计划 -> 评估学习效果 -> 反哺下一轮策略。

### 3.1 北极星目标

提高用户有效学习完成度与复习持续性，而不是只优化生成速度。

### 3.2 KPI（V1）

- 周复习完成率 = 周内已完成复习数 / 周内应复习数
- 7 日记忆保持率 = 7 日内复习评分 >= 3 的占比
- 计划达成率 = 完成任务数 / 计划任务数
- 自动化完成率 = 无人工介入完成任务占比

### 3.3 护栏指标

- 任务失败率
- 人工重试次数
- 人工介入率
- 大任务平均处理时长（接受变长，但要求成功率与可恢复性提升）

## 4. 用户与场景

### 4.1 目标用户

- 校招/考证/课程学习用户
- 高频记录与复习需求用户

### 4.2 典型场景

1. 用户输入笔记、链接或学习需求。
2. 系统进行计划拆分与知识检索。
3. 产出学习卡片与复习任务。
4. 用户完成复习并评分。
5. 系统自动调整后续学习节奏。

## 5. 系统方案：多智能体协作（MCP）

由 Orchestrator 统一调度以下角色：

- Planner：生成执行计划与退出条件
- Retriever：本地优先检索，必要时外部检索
- Auditor：一致性/冲突/补位审计
- Coach：生成学习卡片与讲解内容
- Scheduler：生成与调整复习计划（SM2 + 规则）

说明：V1 为“单 Runner + 多角色职责分层”，V2 再演进到可独立扩展的多 Agent 进程。

## 6. 工作流闭环

### 6.1 主流程

1. 任务入队
2. Plan 生成（JSON steps）
3. 检索上下文（本地 + 外部）
4. 审计冲突与缺口
5. 生成学习卡片
6. 生成/更新复习任务
7. 记录评估并用于下轮决策

### 6.2 异常流程

- 外部检索失败：降级为本地知识增强
- 抓取失败：重试后切换候选来源
- 审计证据不足：标注“待人工确认”
- 多轮失败：输出部分成功并可重入

## 7. 规则化策略（P0）

### 7.1 检索策略

- 默认本地优先
- 相关度不足才触发外部搜索
- 低价值页面（如 sitemap）降权

### 7.2 重试与降级策略

- 关键工具失败最多重试 2 次
- 连续失败触发降级，不阻塞整链路
- 需要人工处理时输出明确提示

### 7.3 计划与执行约束

- 每个任务必须有目标、步骤、工具、退出条件
- 大任务支持断点续跑
- 任务步骤必须可观测、可回放

## 8. 评估机制（P0）

每个任务记录并展示：

- toolCalls
- retries
- degraded（是否发生降级）
- needHumanIntervention（是否需要人工介入）
- durationMs
- success/failure reason

评估信息用于 Dashboard 统计与后续策略优化。

## 9. 数据与接口（V1）

### 9.1 数据对象

- LearningJob：plan / steps / status / lastError
- LearningCard：学习产物（含类型）
- ReviewItem：复习调度状态（SM2）

### 9.2 API 能力

- 任务与步骤查询
- 评估指标查询（后续新增 dashboard 维度）
- 设置页配置（模型、搜索 Key、主题、记忆开关）

## 10. 里程碑与优先级

### P0（立即）

1. 规则化重试/降级策略接入 Runner
2. 任务评估日志标准化（可视化到工作流）
3. 任务失败可恢复（重试/继续执行）

#### P0 当前进展（已完成）

- 已完成：工具失败重试与降级策略（web_search/fetch_url/read_note）
- 已完成：任务评估摘要写入步骤（toolCalls/retries/degraded/needHuman/durationMs）
- 已完成：工作台人工介入动作（中断、继续执行、从失败处重试）
- 已完成：断点恢复基础能力（复用旧 plan、跳过已完成步骤、从失败点后续继续）
- 已完成：轻量多智能体契约（Planner / Retriever / Auditor / Coach / Scheduler）与编排策略表
- 已完成：多 Agent MVP 编排落地（角色级重试/降级、编排摘要步骤、右侧 Agent 列表监控）

### P1（短期）

1. 多智能体协作 MVP：拆分 Planner / Retriever / Auditor / Coach / Scheduler，统一编排协议与任务边界
2. Scheduler 强化：学习计划与复习队列联动（按周目标驱动）
3. 多角色提示词分层与质量评估（输出质量、失败率、人工介入率）

### P2（中期）

1. 多 Agent 进程化拆分（真正的协作编排）
2. 策略自动调参（基于历史效果）
3. 个性化学习路径推荐
4. 可视化增强项（非核心）：评估看板与高级流程可视化按需补齐

## 11. 验收标准（V1）

- 至少 80% 任务可输出完整步骤记录
- 关键工具失败可触发自动重试与降级
- 每个任务可看到评估摘要
- 学习卡片与复习任务可稳定产出并可复用

## 12. 风险与边界

- 大任务耗时上升是预期，需要用成功率与可恢复性抵消
- 外部搜索质量受第三方波动影响，需要保底策略
- 业务指标需要真实埋点与稳定口径，避免伪优化

## 13. 信息架构调整（已决策）

- NextClaw 页右侧区域改为 `Agent 列表`，只展示每个 Agent 任务进程与当前步骤。
- 原 `分析看板` 从 NextClaw 主页面迁出，后续并入学习计划模块（避免与核心执行视图抢空间）。
- 页面策略：NextClaw 专注执行与协作，学习计划模块承接统计分析与长期趋势。
